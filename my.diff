diff --git a/src/nexus_agent/utils/fs.py b/src/nexus_agent/utils/fs.py
index 4cf3e43..6aa8331 100644
--- a/src/nexus_agent/utils/fs.py
+++ b/src/nexus_agent/utils/fs.py
@@ -2,34 +2,42 @@ import os
 from collections.abc import Iterator
 from pathlib import Path

+_SKIP_DIRS = frozenset(
+    {
+        "node_modules",
+        "__pycache__",
+        ".git",
+        "venv",
+        ".venv",
+        "dist",
+        "build",
+    }
+)

-def iter_files(search_path: Path) -> Iterator[Path]:
-    """Lazily iterate files under search_path using os.scandir to avoid OOM from rglob."""
+_ALLOWED_HIDDEN = frozenset({".env", ".gitignore"})
+
+
+def _iter_files_raw(search_path: str) -> Iterator[str]:
     try:
-        with os.scandir(str(search_path)) as it:
+        with os.scandir(search_path) as it:
             for entry in it:
                 try:
                     if entry.is_dir(follow_symlinks=False):
                         # Skip hidden directories (except .env, .gitignore)
-                        if entry.name.startswith(".") and entry.name not in {".env", ".gitignore"}:
+                        if entry.name.startswith(".") and entry.name not in _ALLOWED_HIDDEN:
                             continue
-                        skip_dirs = {
-                            "node_modules",
-                            "__pycache__",
-                            ".git",
-                            "venv",
-                            ".venv",
-                            "dist",
-                            "build",
-                        }
-                        if entry.name in skip_dirs:
+                        if entry.name in _SKIP_DIRS:
                             continue
-                        yield from iter_files(Path(entry.path))
+                        yield from _iter_files_raw(entry.path)
                     elif entry.is_file():
-                        yield Path(entry.path)
+                        yield entry.path
                 except OSError:
                     continue
-    except PermissionError:
-        return
     except OSError:
         return
+
+
+def iter_files(search_path: Path) -> Iterator[Path]:
+    """Lazily iterate files under search_path using os.scandir to avoid OOM from rglob."""
+    for path in _iter_files_raw(str(search_path)):
+        yield Path(path)
